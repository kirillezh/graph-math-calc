name: Graph Math Calc

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:


jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Install dependecies
      run: npm install

    - name: Build the app
      run: npm run build

    - name: Deploy
      run: serve -s build
